<view class="container">
  <view class="box" wx:for="{{service}}" wx:key="index" bindtap="goService" data-index="{{index}}">
    <image src="{{item.url}}"></image>
    <view>{{item.name}}</view>
  </view>
</view>
