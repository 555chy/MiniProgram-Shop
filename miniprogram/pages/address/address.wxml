<view wx:for="{{address}}" wx:key="index" class="box" data-index="{{index}}" bindtap="select">
	<text class="name-mobile">{{item.name}}, {{item.mobile}}</text>
	<text class="address">{{item.district}} {{item.detail}}</text>
</view>
<button class="add" bindtap="add">添加地址</button>